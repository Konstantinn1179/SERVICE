# Универсальный Промпт для создания приложения записи (Telegram Web App)

**Инструкция для AI:**
Скопируй и отправь этот текст мне (AI-ассистенту) в начале нового чата, чтобы я сразу создал проект с правильной архитектурой, избегая прошлых ошибок.

---

## Задача: Создать Telegram Web App для записи на услуги
**Тип проекта:** Приложение для автосервиса/услуг (Ремонт ДВС, Подвеска и т.д.)
**Цель:** Клиент открывает приложение в Telegram, заполняет форму (Имя, Телефон, Детали, Дата/Время), заявка падает в базу, админу приходит уведомление, и создается событие в Google Календаре.

## 🛠 Технический Стек (Строго соблюдать)
1.  **Frontend:** React (Vite) + TypeScript.
2.  **Styling:** Tailwind CSS v4 (Установить локально через npm, **НЕ использовать CDN** во избежание проблем с загрузкой).
3.  **Backend:** Node.js + Express (в папке `/server`).
4.  **Database:** Supabase (PostgreSQL).
5.  **Hosting:** Timeweb Cloud (Node.js Application).
6.  **Integrations:**
    *   Telegram Bot API (`node-telegram-bot-api`).
    *   Google Calendar API (`googleapis`, Service Account).

## 🚀 План разработки (Best Practices)

### 1. Инициализация и Структура
*   Создать проект Vite в корне.
*   Создать папку `server/` для бэкенда.
*   **Важно:** В корневом `package.json` настроить скрипты для деплоя:
    *   `"postinstall": "vite build"` (сборка фронта при установке).
    *   `"start": "node server/server.cjs"` (запуск сервера).

### 2. Frontend (React + Tailwind)
*   Настроить `vite.config.ts`:
    *   `server.proxy`: проксировать `/api` на `http://localhost:5000`.
    *   `server.port`: 3000 (или другой, но согласовать с туннелем).
*   Форма записи должна включать: Имя, Телефон, Авто/Услуга, Дата, Время, Согласие (152-ФЗ).

### 3. Backend (Express)
*   Файл `server/server.cjs`:
    *   API эндпоинты (`/api/bookings`).
    *   Раздача статики (`app.use(express.static('../dist'))`) для продакшена.
    *   Обработка всех остальных запросов (`*`) возвращает `index.html`.

### 4. Интеграции (Секреты через .env)
*   **Telegram:** Бот должен отправлять красивые HTML уведомления админу (`ADMIN_CHAT_ID`).
*   **Google Calendar:**
    *   Использовать Service Account.
    *   Реализовать поддержку передачи ключей через переменную окружения `GOOGLE_SERVICE_ACCOUNT_JSON` (строкой), чтобы не хранить файл ключа в репозитории.
*   **Supabase:** Создать таблицу `bookings` (или специфичную для сервиса).

### 5. Безопасность и Git
*   Сразу создать `.gitignore` в корне и в `server/`.
*   Исключить: `.env`, `node_modules`, `dist`, `*.json` (с ключами).

## 📝 Чек-лист для проверки (Lessons Learned)
1.  [ ] Tailwind установлен локально? (Иначе будет черный экран на iPhone).
2.  [ ] Прокси в Vite настроен? (Иначе CORS ошибки при разработке).
3.  [ ] Сервер отдает статику из `dist`? (Критично для деплоя на Timeweb).
4.  [ ] Переменные окружения используются везде? (Никаких хардкод токенов).
5.  [ ] Gitignore настроен до первого коммита?

---
**Команда для старта:** "Начинай разворачивать структуру проекта согласно этой инструкции. Стек: React, Express, Supabase."
